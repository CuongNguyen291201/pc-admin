#include <stdio.h>
	int main(){
		int i,a[i];
		int n;
		scanf("%d",&n);
		int min;
		
		for(i=0;i<n;i++){
			scanf("%d",&a[i]);
		}
			if(min>a[i]){
				min = a[i];
		printf("%d",min);
		}
	return 0;
	}
