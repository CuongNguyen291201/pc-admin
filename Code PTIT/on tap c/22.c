#include <stdio.h>
#include <math.h>
  int main(){
  	int n;
  	scanf("%d",&n);
  	int a = sqrt(n); 
  	if(a*a == n) printf("la so chinh phuong");
  	else printf("khong la so chinh phuong");
  	return 0;
  }
